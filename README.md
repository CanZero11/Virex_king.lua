# Virex_king.lua
Steal an egg virex hub

## Loadstring

```lua
loadstring(game:HttpGet("https://raw.githubusercontent.com/CanZero11/Virex_king.lua/main/Virex_king.md"))()
```

> The script is stored in `Virex_king.md` but contains Lua source code, so the raw GitHub URL is used directly.
